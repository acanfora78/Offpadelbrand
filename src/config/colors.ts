export const COLORS = {
  cream: "#F5F5F0",
  black: "#080808",
  silver: "#BFC0C2",
  terracotta: "#B94F22",

  /**
   * The exact backdrop colour baked into the official logo artwork.
   * Used wherever the signature sits on a cream field so the plate and the
   * surrounding field merge with no visible seam — the logo file itself is
   * never recoloured.
   */
  logoPlate: "#F5F3EE",
} as const;
